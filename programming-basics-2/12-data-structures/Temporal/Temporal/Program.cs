using Graphs;

namespace Temporal;

internal class Program
{
    //static void Main(string[] args) {
    //    var graph = Graph.MakeGraph(
    //        0, 1,
    //        0, 2,
    //        0, 3,
    //        1, 3,
    //        2, 3);

    //    var wigths = new Dictionary<Edge, double>();
    //    wigths[graph[0, 1]] = 1;
    //    wigths[graph[0, 2]] = 2;
    //    wigths[graph[0, 3]] = 6;
    //    wigths[graph[1, 3]] = 4;
    //    wigths[graph[2, 3]] = 2;

    //    var path = graph.DijkstraAlgorithm(wigths, graph[0], graph[3], new DictionaryPriorityQueue<Node>());
    //    foreach (var e in path)
    //        Console.Write(e.Number + " ");
    //}

    static void Main(string[] args)
    {

    }
}
